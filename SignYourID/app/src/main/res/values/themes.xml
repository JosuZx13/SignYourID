<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SignYourID" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_variant</item>
        <item name="colorOnPrimary">@color/color_on_primary</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_secondary_variant</item>
        <item name="colorOnSecondary">@color/color_on_secondary</item>

        <item name="fontFamily">@font/crimson_text</item>

        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Para ventana emergente a la hora de mostrar la contraseña -->
    <!-- Extiende de Theme.SafePassword para mantener lo ya definido -->
    <style name="FloatingStyleNotTouch" parent="Theme.SignYourID">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="EditText_Login">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColorHint">@color/grey</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:layout_margin">@dimen/entry_welcome</item>
        <item name="android:paddingEnd">@dimen/padding_et_login</item>
        <item name="android:paddingStart">@dimen/padding_et_login</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:textAlignment">textStart</item>
        <item name="android:textSize">@dimen/et_login_text_size</item>
        <item name="android:autoSizeTextType">uniform</item>
    </style>

    <!-- Estilo para los campos de informacion de un trabajador -->
    <style name="InfoStyleWorker">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">info...</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:layout_alignParentStart">true</item>
        <item name="android:layout_marginStart">@dimen/margin_info_tv</item>
        <item name="android:textSize">@dimen/text_size_info</item>
        <item name="android:textStyle">bold</item>
        <item name="android:autoSizeTextType">uniform</item>
        <item name="android:textColor">@color/color_alternative_secondary_variant</item>
    </style>

    <!-- Estilos para los botones del Layout Worker -->
    <style name="InfoStyleButtonWorker">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">2</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/bt_background</item>
    </style>

    <!-- Estilos los "label" de los turnos -->
    <style name="LabelTextViewTurn">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/label_textview</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/color_primary</item>
        <item name="android:layout_alignParentStart">true</item>
        <item name="android:layout_centerInParent">true</item>
    </style>

    <!-- Estilos la informacion de los turnos -->
    <style name="InfoTextViewTurn">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/label_textview</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/color_primary</item>
        <item name="android:layout_alignParentEnd">true</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:textAlignment">center</item>
    </style>

    <!-- Estilos para el contenedor de informacion de un turno -->
    <style name="LinearLayoutInfoTurn">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">3</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:padding">@dimen/padding_info_turn</item>
        <item name="android:baselineAligned">false</item>
    </style>

</resources>